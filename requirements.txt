# Whisper 相關
# 釘版本：large-v3-turbo 與 BatchedInferencePipeline 需 faster-whisper>=1.0；
# 釘死確保部署可重現，避免 pip install 抓到未測過的新版（連帶釘 ctranslate2 後端）
faster-whisper==1.2.1
ctranslate2==4.7.1

# Speaker Diarization
# 釘版本：pyannote.audio 4.x 改用 torchcodec 解碼、需 FFmpeg 共享庫（DLAMI / 靜態 ffmpeg 都沒有）
# → GPU worker 載入即失敗。釘回 prod 驗證過的 3.4.0 + torch/torchaudio 2.5.1（舊解碼後端，免 torchcodec）。
# unpinned 會抓到 bleeding-edge → 「prod 能跑、新機重建就爆」的典型 drift。
torch==2.5.1
torchaudio==2.5.1
pyannote.audio==3.4.0

# 音檔處理
pydub
# audio_service 用 pytz 做時區換算；worker 經 services/__init__ → audio_service import 它，
# 故 worker 的 requirements.txt 也要列（過去只在 requirements-web.txt → fresh worker ImportError）。
pytz

# 中文繁簡轉換
zhconv

# API 相關
fastapi
uvicorn[standard]
python-multipart
motor
aiofiles

# 日期運算（配額週月 refill 用 relativedelta）；原為 pyannote 的 transitive 依賴，
# 計費路徑直接 import 故顯式宣告，避免上游移除時生產環境 ImportError
python-dateutil

# 結構化 logging
structlog

# Google OAuth
google-auth

# LLM API
openai
google-generativeai

# Payments
pycryptodome
httpx

# Error tracking
sentry-sdk[fastapi]
