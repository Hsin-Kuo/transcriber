# ===== AWS AI Worker 依賴（含 ML 模型）=====

# Whisper 相關
faster-whisper

# Speaker Diarization
pyannote.audio

# 音檔處理
pydub

# MongoDB（Worker 直接寫入結果）
pymongo
# motor 與 fastapi 為 reservation_repo 模組頂層 import 所需（worker 走 sync path 不會實際呼叫 async/HTTP 部分）
motor
fastapi

# 中文繁簡轉換
zhconv

# LLM API（標點處理）
openai
google-generativeai

# AWS SDK（S3 下載、SQS 接收）
boto3

# 環境變數
python-dotenv
