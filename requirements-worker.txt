# ===== AWS AI Worker 依賴（含 ML 模型）=====

# Whisper 相關
# 釘版本：large-v3-turbo 與 BatchedInferencePipeline 需 faster-whisper>=1.0；
# 釘死確保 GPU Worker 開機 pip install 可重現，避免抓到未測過的新版（連帶釘 ctranslate2 後端）
faster-whisper==1.2.1
ctranslate2==4.8.0

# Speaker Diarization
pyannote.audio

# 音檔處理
pydub

# MongoDB（Worker 直接寫入結果）
pymongo

# 中文繁簡轉換
zhconv

# LLM API（標點處理）
openai
google-generativeai

# AWS SDK（S3 下載、SQS 接收）
boto3

# 環境變數
python-dotenv
