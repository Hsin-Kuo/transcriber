# ===== AWS Web Server 依賴（不含 ML 模型）=====

# 音檔處理（ffmpeg 前處理、合併等）
pydub

# 時區
pytz

# API 相關
fastapi
uvicorn[standard]
python-multipart

# 結構化 logging（JSON output + request_id contextvars）
structlog

# Google OAuth
google-auth

# LLM API（標點處理由 Worker 執行，但 Web Server 也用到 PunctuationProcessor 的部分邏輯）
openai
google-generativeai

# MongoDB
pymongo
motor

# 環境變數
python-dotenv

# JWT
python-jose[cryptography]
passlib[bcrypt]

# 藍新金流加密
pycryptodome

# Email 驗證
email-validator

# Email 模板
jinja2

# Resend (Email 服務)
resend

# Stripe (訂閱付款)
stripe

# AWS SDK
boto3

# BSON (ObjectId)
# pymongo 已包含 bson

# PDF 生成（server-side multi-language CJK PDF；字體在 src/utils/pdf/fonts/）
reportlab>=4.4,<5.0
