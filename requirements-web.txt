# ===== AWS Web Server 依賴（不含 ML 模型）=====

# 音檔處理（ffmpeg 前處理、合併等）
pydub

# 時區
pytz

# API 相關
fastapi
uvicorn[standard]
python-multipart
# 分片上傳 streaming write（src/routers/uploads.py、transcriptions.py）
aiofiles

# 結構化 logging（JSON output + request_id contextvars）
structlog

# Google OAuth
google-auth

# LLM API（標點處理由 Worker 執行，但 Web Server 也用到 PunctuationProcessor 的部分邏輯）
openai
google-generativeai

# MongoDB
pymongo
motor

# 環境變數
python-dotenv

# JWT
python-jose[cryptography]
passlib[bcrypt]

# 藍新金流加密
pycryptodome

# Email 驗證
email-validator

# Email 模板
jinja2

# Resend (Email 服務)
resend

# 91APP 金流 / SmilePay 發票的 HTTP client（先前靠 openai SDK 傳遞依賴撿到，顯式列出）
httpx

# SmilePay 電子發票：回應是 XML，用 defusedxml 解析（防 XXE）
defusedxml

# AWS SDK
boto3

# BSON (ObjectId)
# pymongo 已包含 bson

# PDF 生成（server-side multi-language CJK PDF；字體在 src/utils/pdf/fonts/）
reportlab>=4.5.1,<5.0
svglib==1.5.1  # 收據 logo 走向量 SVG（放大不糊）；>=1.6 會拉 rlpycairo→pycairo 要編系統 cairo，別升

# Error tracking——⚠️ 一定要在本檔（web 部署只裝 requirements-web.txt）。
# 2026-09-01 go-live 才發現只在 requirements.txt（worker 用）→ prod web 的
# sentry_sdk 缺席，SENTRY_DSN 白設、金流/發票所有 Sentry 告警靜默 no-op。
sentry-sdk[fastapi]
